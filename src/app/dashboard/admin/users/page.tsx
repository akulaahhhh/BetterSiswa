import { jacques_francois } from "@/app/fonts";
// import { jacques_francois_shadow, jacques_francois } from "@/app/fonts";

export default function Users() {
  return (
    <>
    <main className="w-full min-h-screen flex flex-col max-w-7xl mx-auto px-4 ">
        <div className="pt-9 flex justify-center">
          <h2 className={`${jacques_francois.className} text-xl`}>Users</h2>
        </div>
      </main>
    </>
  );
}
